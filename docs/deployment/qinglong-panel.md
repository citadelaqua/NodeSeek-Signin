# 青龙面板

在青龙面板中执行以下命令克隆本仓库：

```bash
ql repo https://github.com/yowiv/NodeSeek-Signin.git
```

克隆完成后，青龙会自动检测仓库中的所有 `.py` 文件。在任务列表中找到 `src/nodeseek/sign.py` 并启用即可。

> 其余变量配置（Cookie、验证码服务、通知渠道等）请参考 [config.md](../configuration/config.md)。
